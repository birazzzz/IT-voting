{
  "name": "impact-token-voting",
  "version": "1.0.0",
  "description": "Impact Token Voting System",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@netlify/functions": "^1.0.0"
  }
}
